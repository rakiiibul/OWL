<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.owl">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/owl"
        android:label="@string/app_name"
        android:roundIcon="@drawable/owl"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash_act" android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".HOME"
            android:label="@string/title_activity_h_o_m_e"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".Login">

        </activity>
        <activity android:name=".activity.post_detailact"></activity>
        <activity android:name=".signup_activity"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>